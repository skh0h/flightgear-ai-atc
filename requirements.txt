# NOTE: exact package names and versions should be confirmed against Google's
# official Gemini Python SDK docs at implementation time.
# As of mid-2025 the SDK package is `google-genai`; verify before pinning.
google-genai
python-dotenv
pytest
